You are a senior software architect and front-end engineer.

I want to design the HOME section of my web app as an
"Architecture Workspace", not a feature page.

Goal:
The Home page should visually explain and mock up
how a large-scale modular system is architected.

This is NOT a real code editor.
This is a visual / conceptual architecture builder.

Core features to design:

1️⃣ Architecture Diagram Area (Main Focus)
- Visual system diagram (boxes + arrows)
- Layers such as:
  - UI Layer
  - Core / State Layer
  - Modules
  - External APIs
- Connections should clearly show data flow
- Diagram is illustrative (mockup style), not functional

2️⃣ Code Structure Visualization
- Show a large project folder structure, for example:
  src/
    core/
    modules/
    services/
    utils/
    i18n/
- Displayed as:
  - Tree view
  - Or code-style block
- This is read-only and conceptual

3️⃣ Data Flow Explanation
- Step-by-step flow such as:
  User Action → Controller → API → Normalize → Render
- Presented as:
  - Timeline
  - Vertical steps
  - Or horizontal flow cards

4️⃣ Architecture Principles Section
Short, strong principles like:
- Modular & independent features
- Separation of concerns
- API isolation
- Scalable structure
- UI does not depend on data source

5️⃣ Tech Stack Overview
Minimal icons or cards showing:
- HTML
- CSS
- Vanilla JavaScript
- REST APIs
- Modular architecture
- i18n-ready

UI / UX requirements:
- Dark mode design
- Minimal, professional, engineering-focused
- Looks like a system design tool, not a blog
- Clean spacing, subtle animations
- Card-based layout

Important constraints:
- This is a MOCKUP / VISUALIZATION only
- No real code execution
- No editors
- No dependency on other app sections
- Home page must stand alone conceptually

Output:
- Page layout description
- Suggested component structure
- Optional HTML structure (static)
- Explanation of how the architecture visualization is presented

Do NOT include Weather or any other feature.
Focus only on architecture thinking and system design.
